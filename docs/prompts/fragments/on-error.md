# 에러 대응 규칙

1. **에러난 부분만을 해결하기 위한 코드를 작성할 것.** 이미 성공한 코드를 다시 작성하지 말 것
2. **에러난 부분부터 이어서 작성할 것.** 문서/슬라이드/워크북 생성 코드(`Add()`, `AddSlide()`, `FileNew` 등)가 이미 성공했으면 절대 다시 호출하지 말 것 — 중복 생성됨
3. **에러를 해결한 후, 프롬프트에 추가하면 좋을 내용을 `prompt-patch` 블록에 남길 것.** 앱이 수집하여 향후 프롬프트 개선에 활용한다.
